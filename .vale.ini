# Vale configuration — https://vale.sh/docs/topics/config
# Only Vale's built-in checks (spelling, repetition) — no external style packs.
StylesPath = .vale/styles
MinAlertLevel = suggestion
Vocab = Docs

[*.md]
BasedOnStyles = Vale
